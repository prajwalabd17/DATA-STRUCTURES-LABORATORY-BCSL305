/*
Given a File of N employee records with a set K of Keys (4-digit) which uniquely determine
the records in file F. Assume that file F is maintained in memory by a Hash Table (HT) of m
memory locations with L as the set of memory addresses (2-digit) of locations in HT. Let the
keys in K and addresses in L are Integers. 
Develop a Program in C that uses Hash function H:K →L as H(K)=K mod m (remainder method), 
and implement hashing technique to map a given key K to the address space L. 
Resolve the collision (if any) using linear probing.
*/

#include<stdio.h>
#include<stdlib.h>
int key[20], n, m;
int *ht, index;
int count = 0;
void insert(int key)
{
    int home_index;
    home_index = key % m;   // Original hash address
    index = home_index;
    if (ht[index] != -1)
    {
        printf("\nCollision occurred for key %d at address %d", key, index);
    }
    while (ht[index] != -1)
    {
        index = (index + 1) % m;
    }
    if (index != home_index)
    {
        printf("\nKey %d inserted at address %d using linear probing", key, index);
    }
    ht[index] = key;
    count++;
}
void display()
{
    int i;
    if (count == 0)
    {
        printf("\nHash Table is empty");
        return;
    }
    printf("\n\nHash Table contents are:\n");
    for (i = 0; i < m; i++)
        printf("T[%d] --> %d\n", i, ht[i]);
}
void main()
{
    int i;
    printf("\nEnter the number of employee records (N): ");
    scanf("%d", &n);
    printf("\nEnter the two-digit memory locations (m) for the hash table: ");
    scanf("%d", &m);
    ht = (int *)malloc(m * sizeof(int));
    for (i = 0; i < m; i++)
        ht[i] = -1;
    printf("\nEnter the four-digit key values (K) for N Employee Records:\n");
    for (i = 0; i < n; i++)
        scanf("%d", &key[i]);
    for (i = 0; i < n; i++)
    {
        if (count == m)
        {
            printf("\n~~~Hash table is full. Cannot insert the record %d key~~~", i + 1);
            break;
        }
        insert(key[i]);
    }
    display();
}

/*Output:
Enter the number of employee records (N): 4

Enter the two-digit memory locations (m) for the hash table: 10

Enter the four-digit key values (K) for N Employee Records:
2003
4502
4203
2002

Collision occurred for key 4203 at address 3
Key 4203 inserted at address 4 using linear probing
Collision occurred for key 2002 at address 2
Key 2002 inserted at address 5 using linear probing

Hash Table contents are:
T[0] --> -1
T[1] --> -1
T[2] --> 4502
T[3] --> 2003
T[4] --> 4203
T[5] --> 2002
T[6] --> -1
T[7] --> -1
T[8] --> -1
T[9] --> -1*/
